pipeline {
    agent any

    stages {
        stage('Greeting') {
            steps {
                sh 'sudo yum install git -y'
                sh 'git 'https://github.com/Pritam-Khergade/student-ui.git'
            }
        }
    }
}
